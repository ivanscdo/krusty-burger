<li>
    <p>
        {{id}}. {{burger_name}}

        <button class="devour" data-id="{{id}}" data-devour="{{devour}}">
            {{#if devour}}Devoured!
            {{else}}Devour!
            {{/if}}
            {{!-- {{#unless devour}} Devour!
            {{/unless}} --}}
        </button>
    </p>
</li>


    {{!-- <li>
        <p>
            {{this.id}}. {{this.burger_name}}
        </p>
    </li> --}}